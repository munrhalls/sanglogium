0 dumb verification calls, expensive commands running lints, running any npm rebuilds or npm run build or npm run dev or any stupid debilitatingly dump shit like that It's all stupid and destroys work Why? Because it wastes time on doing stupid, low trust worthiness actions and attempts at verification before Tracer bullets are built and live checked It takes a few seconds to do a live check on a simple small single responsibility tracer bullet and it's infinitely more trustworthy and valuable than any stupid dumb automated lint command or stupid typescript check command or even more so stupid fucking npm run build Everything takes and costs time. And if that time is used on stupid shit, then it is wasted. And everything about building must be around tracer bullets and quick life checks by human on the dev server that just keeps running and is updated immediately as the source code is updated and the AI doesn't need to worry about it. So the AI, the only thing it should do when building a tracer bullet is to just get the prompt and just build the source code for exactly that tracer bullet in leanest manner possible with least steps possible needed to build it