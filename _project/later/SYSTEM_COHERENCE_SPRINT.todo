[] systematically scan entire homepage, component by component
    [] typography - exceptions are allowed - but confirm them
    [] typography - false exceptions (random) -> turn to alias usage
    [] spacing - exceptions are allowed - but confirm them
    [] spacing - false exceptions (random) -> turn to alias usage

    [] colors - same

