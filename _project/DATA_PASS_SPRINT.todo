Replace the entire content of _project/DATA_PASS_SPRINT.todo
with exactly this:

---

Homepage Data Pass Sprint:

  Foundation (already complete):
    ✔ homepageData schema created with all fields @done(26-03-19 08:26)
    ✔ homepageData added to Sanity schema configuration @done(26-03-19 08:26)
    ✔ Singleton desk structure configured @done(26-03-19 08:26)
    ✔ export const revalidate = 3600 in app/(store)/page.tsx @done(26-03-19 08:26)
    ✔ Hero confirmed untouched @done(26-03-19 08:26)
    ✔ Featured data pass complete and working @done(26-03-19 08:26)

  ProductSpotlight1:
    ✔ getFeaturedSpotlight1.ts created in component folder @done(26-03-19 09:00)
    ✔ Interface covers: promoTitle, promoSubtitle, promoText, @done(26-03-19 09:00)
        productRef._id, name, brand, displayPrice, image.asset.url
    ✔ GROQ query: *[_type=="homepageData"][0].spotlight1Data @done(26-03-19 09:00)
        with productRef->{ fields }
    ✔ Component is async server component @done(26-03-19 09:00)
    ✔ Real data renders — no lorem ipsum, no mock data @done(26-03-19 09:00)
    ✔ Null guard present — renders null if no data @done(26-03-19 09:00)
    ✔ No console errors @done(26-03-19 09:01)
    ✔ Renders at 1280px @done(26-03-19 09:01)
    ✔ Renders at 375px @done(26-03-19 09:01)

  ProductSpotlight2:
    [ ] getSpotlight2.ts created in component folder
    [ ] Reuses SpotlightProduct interface from Spotlight1 or defines own
    [ ] GROQ query: *[_type=="homepageData"][0].spotlight2Data
        with productRef->{ fields }
    [ ] Component is async server component
    [ ] Real data renders — no lorem ipsum, no mock data
    [ ] Null guard present
    [ ] No console errors
    [ ] Renders at 1280px
    [ ] Renders at 375px

  ProductSpotlight3:
    [ ] getSpotlight3.ts created in component folder
    [ ] GROQ query: *[_type=="homepageData"][0].spotlight3Data
        with productRef->{ fields }
    [ ] Component is async server component
    [ ] Real data renders — no lorem ipsum, no mock data
    [ ] Null guard present
    [ ] No console errors
    [ ] Renders at 1280px
    [ ] Renders at 375px

  IemsGallery:
    [ ] getIemProducts.ts created in component folder
    [ ] Interface: _id, name, brand, displayPrice, image.asset.url
    [ ] GROQ query: *[_type=="homepageData"][0].iemsGallery[]->{fields}
    [ ] Component is async server component
    [ ] Real data renders — no mock data
    [ ] Null/empty guard present
    [ ] No console errors
    [ ] Renders at 1280px — 4 column grid visible
    [ ] Renders at 375px — 2 column grid visible

  NewestRelease:
    [ ] getNewestRelease.ts created in component folder
    [ ] Interface covers: promoTitle, promoSubtitle, promoText,
        productRef._id, name, brand, image.asset.url, gallery[].asset.url
    [ ] GROQ query: *[_type=="homepageData"][0].newestReleaseData
        with productRef->{..., gallery[]{asset->{url}}}
    [ ] mainImage and gallery combined into single images array
    [ ] Component is async server component
    [ ] Real data renders — no mock data
    [ ] Null guard present
    [ ] No console errors
    [ ] Renders at 1280px
    [ ] Renders at 375px

  Dacs:
    [ ] getDacProducts.ts created in component folder
    [ ] Interface: _id, name, brand, displayPrice, image.asset.url
    [ ] GROQ query: *[_type=="homepageData"][0].dacs[]->{fields}
    [ ] Component is async server component
    [ ] Real data renders — no mock data
    [ ] Null/empty guard present
    [ ] No console errors
    [ ] Renders at 1280px
    [ ] Renders at 375px

  Accessories:
    [ ] getAccessoryProducts.ts created in component folder
    [ ] Interface: _id, name, brand, displayPrice, image.asset.url
    [ ] Three GROQ queries in one async function using Promise.all:
        accessoriesCables[]->{fields}
        accessoriesEarpads[]->{fields}
        accessoriesStorage[]->{fields}
    [ ] Component is async server component
    [ ] All three carousels render real data
    [ ] Null/empty guard on each carousel independently
    [ ] No console errors
    [ ] Renders at 1280px
    [ ] Renders at 375px

  Final validation:
    [ ] Sanity Studio homepage singleton populated with real products
    [ ] All 8 components show real data simultaneously on homepage
    [ ] No mock data or lorem ipsum visible anywhere
    [ ] No console errors on full page load
    [ ] Homepage renders at 375px — no broken sections
    [ ] Homepage renders at 1280px — no broken sections
    [ ] Hero confirmed untouched — git diff shows no Hero changes

---

CONSTRAINTS:
- Replace the entire file with exactly this content
- Do not touch any other file