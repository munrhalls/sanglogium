Typography Sprint:

  Test page setup:
    ✔ /app/design-system-test/typography/page.tsx created @done(26-03-16 20:26)
    [ ] Page renders all 8 scale steps in hierarchy order
    [ ] Each step labeled with: name, clamp formula, px range, intended role
    [ ] Page visible at localhost:3000/design-system-test/typography


  Scale verification at 375px (resize browser or DevTools):
    [ ] display-1 distinct from display-2 — clearly larger
    [ ] display-2 distinct from h1 — clearly larger
    [ ] h1 distinct from h2 — clearly larger
    [ ] h2 distinct from h3 — clearly larger
    [ ] h3 distinct from h4 — clearly larger
    [ ] h4 distinguished from body at 375px by letterSpacing and weight,
    not size — this is acceptable and intentional
    [ ] body distinct from small — clearly larger
    [ ] All steps readable at 375px — nothing feels too small for its role
    [ ] display-2 larger than h1 at 375px — hierarchy does not invert at mobile


  Scale verification at 768px:
    [ ] All 8 steps remain distinct
    [ ] Fluid interpolation feels smooth — no step jumps awkwardly

  Scale verification at 1280px:
    [ ] All 8 steps distinct
    [ ] display-1 at desktop feels appropriately large for a hero headline
    [ ] body at desktop remains comfortable reading size

  Role mapping resolution:
    [ ] Hero subheadline role determined: which scale step is correct
    [ ] Decision documented: why that step, not h4
    [ ] Named roles written to DESIGN_SYSTEM.md:
        [ ] hero-headline role mapped
        [ ] hero-subheadline role mapped
        [ ] section-headline role mapped
        [ ] card-title role mapped
        [ ] metadata role mapped

  Weight and spacing verification (on test page):
    [ ] font-bold on display-1 and display-2 looks intentional
    [ ] font-semibold on h1 and h2 looks intentional
    [ ] letter-spacing on h3 and h4 (0.05em, 0.1em) reads correctly
    [ ] letter-spacing on display-1 (-0.02em) reads correctly — tight, intentional
    [ ] body letter-spacing (0em) reads correctly
    [ ] Size ratios between adjacent steps fall between 1.25 and 1.45 at desktop
    (verify: display-2/h1, h1/h2, h2/h3, h3/h4, h4/body)

  Multiline verification:
    [ ] display-1 at 2+ lines: line-height 1.1 does not cause line overlap
    [ ] h1 at 2+ lines: line-height 1.2 looks correct
    [ ] body at 3+ lines: line-height 24px reads comfortably

  text-cap verification:
    [ ] .text-cap applied to display-1: top trim visible (no space above cap)
    [ ] .text-cap applied to body: trim visible
    [ ] gap-2 between two .text-cap elements: 8px visual gap confirmed in DevTools

  Final:
    [ ] Any adjusted clamp values saved to tailwind.config.ts
    [ ] Role mappings written to DESIGN_SYSTEM.md
    [ ] Test page deleted
    [ ] Typography frozen — tailwind.config.ts fontSize not touched during component sprint
