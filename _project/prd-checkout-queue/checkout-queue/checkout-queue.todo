# Queue Skeleton Requirements

## Scope
Minimal working queue for basket reservation requests to establish cover & move before any complexity.

## Out of Scope
Everything not in DoD (priority queues, idempotency, retry logic, circuit breaker, concurrency control, fingerprinting).

# Aftermath target
After completion: single Upstash Redis queue running 24/7, processing basket reservation requests one at a time with atomic operations, type-safe UI↔Redis↔CMS structure fidelity. All 6 DoD items verified with human-observable console logs.

## Definition of Done
✔ Test suite: 1 E2E, 3 Integration, 1 Unit, 0 mocks, real infrastructure only, in /tests/checkout-queue-skeleton/<unit/integration/e2e> folder @done(26-04-18 16:18)

    ✔ Test (Integration): Sequential FIFO processing @done(26-04-18 11:47)
        ✔ Trace: 9 curl requests → queue processor → verify sequential logs @done(26-04-18 11:47)
        ✔ Setup: Queue processor running, Redis connected @done(26-04-18 11:47)
        ✔ Start conditions: Upstash connected, queue processor active @done(26-04-18 11:47)
        ✔ Assertion: Logs show "Processing 1 → Complete 1 → Processing 2 → Complete 2 → ... → Processing 9 → Complete 9" @done(26-04-18 11:47)
        ✔ Justification: Verifies core queue behavior prevents race conditions @done(26-04-18 11:47)
        ✔ Real feedback: Detects parallel processing bugs, validates atomic operations @done(26-04-18 11:47)

    ✔ Test (E2E): INPUT/OUTPUT flow -> in queue-skeleton-e2e-test-page.todo @done(26-04-18 16:18)


    ✔ Test (Integration): Type/structure fidelity @done(26-04-18 11:54)
        ✔ Trace: Malformed request → structure validation → error @done(26-04-18 11:54)
        ✔ Setup: Queue processor with type checking enabled @done(26-04-18 11:54)
        ✔ Start conditions: Upstash connected, type validation active @done(26-04-18 11:54)
        ✔ Assertion: Logs show 6 structure checks fail in order, error returned @done(26-04-18 11:54)
        ✔ Justification: Prevents data corruption from malformed input @done(26-04-18 11:54)
        ✔ Real feedback: Catches type mismatches before they corrupt queue/CMS @done(26-04-18 11:54)

    ✔ Test (Unit): TypeScript type validation @done(26-04-18 11:55)
        ✔ Trace: Malformed request → TypeScript type check → compile error @done(26-04-18 11:55)
        ✔ Setup: TypeScript compiler @done(26-04-18 11:55)
        ✔ Start conditions: Source code with type annotations @done(26-04-18 11:55)
        ✔ Assertion: Compile fails with type error for malformed request @done(26-04-18 11:55)
        ✔ Justification: Catches type errors at compile time, not runtime @done(26-04-18 11:55)
        ✔ Real feedback: Prevents type-related bugs before deployment @done(26-04-18 11:55)


✔ Single production queue (no other instances), 0 cost, runs 24/7: **Upstash Redis (Free Tier) + Redis Streams** @done(26-04-17 19:52)
    ✔ Verification: `console.log('TRACE: Upstash connection established', { url, status })` on startup @done(26-04-17 19:52)
    ✔ Verification: `console.log('TRACE: Queue health check', { connected, latency })` every 60s @done(26-04-17 19:52)
    ✔ Queue not duplicated on subsequent npm run dev or npm run start calls @done(26-04-17 19:52)
        ✔ Verification: Run `npm run dev`, stop, run `npm run dev` again - should see "TRACE: Upstash connection established" and "TRACE: Queue health check" logs once per server start, not twice @done(26-04-17 19:52)

✔ Sequential FIFO processing, atomic operations: One request at a time, atomic (wait 100% complete before starting next) @done(26-04-18 09:26)
    ✔ Verification: `console.log('TRACE: Processing request', { requestId, queuePosition })` at start @done(26-04-18 09:26)
    ✔ Verification: `console.log('TRACE: Request complete', { requestId, duration })` at end @done(26-04-18 09:25)
    ✔ Test: `for i in {1..9}; do curl -X POST http://localhost:3000/api/checkout-queue -H "Content-Type: application/json" -d "{\"publicBasket\":[{\"_id\":\"prod-$i\",\"quantity\":$i}]}" & done; wait`, verify logs show exactly: "Processing request 1 → "Request complete 1" → "Processing request 2 → "Request complete 2" → ... → "Processing request 9" → "Request complete 9" @done(26-04-18 09:25)

✔ INPUT/OUTPUT streams flow: Basket reservation requests stream from ui → queued one at a time → atomic → stream to Sanity CMS → on response, pop currently processed from queue -> pass response back to UI @done(26-04-18 16:00)
    ✔ Incrementally build, run and verify queue-skeleton-e2e-test-page.todo @done(26-04-18 16:00)


✔ Type/structure fidelity: UI request structure → Redis queue structure → CMS request structure, CMS response structure → Redis queue structure → UI response structure @done(26-04-18 11:32)
    ✔ Verification: `console.log('TRACE: UI request structure exists', { structure: 'UIRequest', valid: true })` before sending to Redis @done(26-04-18 11:32)
    ✔ Verification: `console.log('TRACE: Redis queue structure exists', { structure: 'RedisQueue', valid: true })` before sending to CMS @done(26-04-18 11:32)
    ✔ Verification: `console.log('TRACE: CMS request structure exists', { structure: 'CMSRequest', valid: true })` before processing @done(26-04-18 11:32)
    ✔ Verification: `console.log('TRACE: CMS response structure exists', { structure: 'CMSResponse', valid: true })` before returning @done(26-04-18 11:32)
    ✔ Verification: `console.log('TRACE: Redis queue structure exists', { structure: 'RedisQueue', valid: true })` before returning to UI @done(26-04-18 11:32)
    ✔ Verification: `console.log('TRACE: UI response structure exists', { structure: 'UIResponse', valid: true })` before displaying @done(26-04-18 11:32)
    ✔ Test: Send request, verify logs show 6 structure existence checks in order @done(26-04-18 11:32)

✔ TypeScript: Strict request/response type handling @done(26-04-18 11:33)
    ✔ Verification: `console.log('TRACE: Type validation', { requestId, requestType, responseType })` on each request/response @done(26-04-18 11:33)
    ✔ Test: Send malformed request, verify type validation error in logs @done(26-04-18 11:34)
