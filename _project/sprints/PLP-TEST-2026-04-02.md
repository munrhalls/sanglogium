# Sprint: Products Page Test Suite Implementation

> **Sprint Name:** PLP-TEST-2026-04-02
> **Target State:** Minimal robust test suite for `/products/[...slug]` route
> **Scope Lock Date:** 2026-04-02
> **Estimated Duration:** 60-75 minutes
> **Sprint Type:** Testing Infrastructure

---

## Sprint Metadata

### Scope Lock Rules (MANDATORY — Do Not Violate)
- **NO** modifications to `globals.css` — testing only, no styling changes
- **NO** modifications to existing component logic — only add test utilities
- **NO** changes to VFS data layer — 67 existing tests remain untouched
- **NO** homepage or other page tests — PLP scope only
- **NO** visual regression tests — out of scope (separate concern)
- **YES** test utilities in `tests/utils/`
- **YES** integration tests in `tests/integration/products/`
- **YES** E2E tests in `tests/e2e/products-page/`

### Baseline State (Pre-Sprint)
```bash
# Verify before starting
npx vitest run tests/catalogue/vfs.test.ts --reporter=verbose
# Expected: 67 tests passing
# Expected: ~10s execution time
```

---

## Scope Contracts (SC)

### SC1: Test Utilities Foundation — G-01 Coverage

**Gap Coverage:** G-01 — No products-page-helpers.ts exists

**Target State:**
Complete test utility module providing selectors, navigation helpers, and test data constants for all PLP tests.

**DoD (Sequenced):**
- [ ] **Pass 1:** File skeleton created at `tests/utils/products-page-helpers.ts`
- [ ] **Pass 2:** SELECTORS object with all data-testid mappings
- [ ] **Pass 2:** `navigateToCategory()` helper function
- [ ] **Pass 2:** `TEST_CATEGORIES` constant with test slugs
- [ ] **Pass 2:** `viewports` configuration for RWD testing
- [ ] **Pass 3:** Import verification (no TypeScript errors)
- [ ] **Pass 3:** Export completeness check

**Build Gate:**
```bash
# Pass 1-2 verification
npx tsc --noEmit tests/utils/products-page-helpers.ts
# Pass 3 verification  
npm run build
```

**Delegation:**
- **Execution:** `/implement "Create products-page-helpers.ts with SELECTORS, navigation, TEST_CATEGORIES, viewports"`
- **Verify:** `/test --file tests/utils/products-page-helpers.ts`

---

### SC2: Integration Tests — G-02 Coverage

**Gap Coverage:** G-02 — No integration tests for PLP components

**Target State:**
2 integration test files verifying component behavior in isolation.

**DoD (Sequenced):**
- [ ] **Pass 1:** Test file skeleton: `tests/integration/products/ProductGrid.integration.test.tsx`
- [ ] **Pass 1:** Test file skeleton: `tests/integration/products/CategoryPageClient.integration.test.tsx`
- [ ] **Pass 2:** Test I-01: ProductGrid renders products with correct data
- [ ] **Pass 2:** Test I-02: ProductGrid shows empty state when no products
- [ ] **Pass 2:** Test I-03: CategoryPageClient displays correct product count (singular/plural)
- [ ] **Pass 3:** All 3 tests pass in Vitest
- [ ] **Pass 3:** No console errors during test execution

**Build Gate:**
```bash
# Pass 1-2 verification
npx vitest run tests/integration/products/ --reporter=verbose
# Expected: 3 tests passing
# Expected: ~15s execution time

# Pass 3 verification
npm run build
```

**Delegation:**
- **Execution:** `/implement "Create ProductGrid.integration.test.tsx with render and empty state tests"`
- **Execution:** `/implement "Create CategoryPageClient.integration.test.tsx with count display test"`
- **Verify:** `/test --suite integration/products`

---

### SC3: E2E Critical Flows — G-03 Coverage

**Gap Coverage:** G-03 — No dedicated PLP E2E tests

**Target State:**
6 E2E tests covering critical user journeys across desktop and mobile viewports.

**DoD (Sequenced):**
- [ ] **Pass 1:** Test file skeleton: `tests/e2e/products-page/critical-flows.spec.ts`
- [ ] **Pass 1:** Test utilities import from products-page-helpers.ts
- [ ] **Pass 2:** Test E2E-01: Category page loads and displays products
- [ ] **Pass 2:** Test E2E-02: Invalid category slug shows 404
- [ ] **Pass 2:** Test E2E-03: Empty category shows "no products" message
- [ ] **Pass 2:** Test E2E-04: Mobile filter drawer opens and closes
- [ ] **Pass 2:** Test E2E-05: Sort selection updates URL and results
- [ ] **Pass 2:** Test E2E-06: Loading skeleton visible during data fetch
- [ ] **Pass 3 — Layer 2:** Desktop viewport (1280×720) all tests pass
- [ ] **Pass 3 — Layer 2:** Mobile viewport (375×844) drawer test passes
- [ ] **Pass 3 — Layer 3:** Screenshots captured for manual verification
- [ ] **Pass 3 — Layer 4:** CI execution time < 90 seconds for all 6 tests

**Build Gate:**
```bash
# Pass 1-2 verification
npx playwright test tests/e2e/products-page/critical-flows.spec.ts --reporter=list
# Expected: 6 tests passing
# Expected: ~60s execution time

# Pass 3 verification
npm run build
```

**Delegation:**
- **Execution:** `/implement "Create critical-flows.spec.ts with 6 E2E tests covering navigation, 404, empty state, mobile drawer, sort, and loading skeleton"`
- **Build:** `/build E2E Pass 3 Layer 2 Desktop`
- **Build:** `/build E2E Pass 3 Layer 2 Mobile`
- **Verify:** `/test --suite e2e/products-page`

---

## RWD Strategy

### Viewport Matrix (Per Test)
| Test | Desktop (1280px) | Mobile (375px) | Implementation |
|------|------------------|----------------|----------------|
| E2E-01 Category loads | ✅ Test | ❌ Skip | Default viewport |
| E2E-02 Invalid 404 | ✅ Test | ❌ Skip | Default viewport |
| E2E-03 Empty state | ✅ Test | ❌ Skip | Default viewport |
| E2E-04 Mobile drawer | ❌ N/A | ✅ Test | `setViewportSize(375, 844)` |
| E2E-05 Sort URL | ✅ Test | ❌ Skip | Default viewport |
| E2E-06 Skeleton | ✅ Test | ❌ Skip | Default viewport |

**Note:** Only E2E-04 requires mobile viewport (drawer is mobile-only component). Other tests verify functionality that works identically across viewports.

---

## Files at Risk of Regression

| File | Risk Level | Mitigation |
|------|------------|------------|
| `tests/utils/product-detail-helpers.ts` | Low | Reference pattern only — no shared code |
| `playwright.config.ts` | Low | No changes — use existing 7-project config |
| `vitest.config.mts` | Low | No changes — jsdom already configured |
| `tests/catalogue/vfs.test.ts` | Low | Baseline verification — must pass before/after |
| `app/(store)/products/[...slug]/*.tsx` | None | Zero component modifications required |

---

## Regression Containment Plan

### Pre-Sprint Baseline
```bash
# Capture baseline before sprint
npx vitest run tests/catalogue/vfs.test.ts --reporter=verbose > test-baseline-vfs.log
# Verify: 67 tests passing

npm run build > build-baseline.log
# Verify: Build succeeds
```

### During Sprint Checks
| Checkpoint | Command | Expected |
|------------|---------|----------|
| After SC1 | `npx tsc --noEmit tests/utils/products-page-helpers.ts` | No errors |
| After SC2 | `npx vitest run tests/integration/products/` | 3 tests pass |
| After SC3 | `npx playwright test tests/e2e/products-page/` | 6 tests pass |
| Final | `npm run build` | Build succeeds |

### Post-Sprint Verification
```bash
# Full test suite verification
npx vitest run tests/catalogue/vfs.test.ts tests/integration/products/
npx playwright test tests/e2e/products-page/

# Expected totals:
# - VFS: 67 tests (unchanged)
# - Integration: 3 tests (new)
# - E2E: 6 tests (new)
# - Total new: 9 tests + utilities
```

---

## Execution Order (MANDATORY)

```
PHASE 1: Foundation (SC1)
├── /implement SC1
├── /test SC1
└── Build gate: TypeScript check

PHASE 2: Integration (SC2)
├── /implement SC2 — Test I-01, I-02 (ProductGrid)
├── /implement SC2 — Test I-03 (CategoryPageClient)
├── /test SC2
└── Build gate: Vitest pass

PHASE 3: E2E (SC3)
├── /implement SC3 — E2E-01, E2E-02, E2E-03
├── /implement SC3 — E2E-04 (mobile drawer)
├── /implement SC3 — E2E-05, E2E-06
├── /build SC3 — Desktop viewport
├── /build SC3 — Mobile viewport
├── /test SC3
└── Build gate: Playwright pass + npm run build

PHASE 4: Lock
├── /test — Full suite
├── Capture evidence
└── Sprint complete
```

---

## Evidence Log (To Be Filled During Execution)

| Phase | Command | Result | Timestamp |
|-------|---------|--------|-----------|
| Baseline | `npx vitest run tests/catalogue/vfs.test.ts` | [PENDING] | — |
| SC1 Gate | `npx tsc --noEmit tests/utils/products-page-helpers.ts` | [PENDING] | — |
| SC2 Gate | `npx vitest run tests/integration/products/` | [PENDING] | — |
| SC3 Gate | `npx playwright test tests/e2e/products-page/` | [PENDING] | — |
| Final Gate | `npm run build` | [PENDING] | — |

---

## Anti-Patterns to Avoid

- ❌ **Don't** add tests for every filter permutation — combinatorial explosion
- ❌ **Don't** mock Sanity data in E2E tests — test real data flow or skip with cmsAvailable pattern
- ❌ **Don't** add visual regression — separate concern, high maintenance
- ❌ **Don't** modify components to add test IDs — they're already present
- ❌ **Don't** mix Pass 1 skeleton with Pass 3 test logic

---

## Sprint Completion Criteria

- [ ] All 3 Scope Contracts have passing DoDs
- [ ] 67 baseline VFS tests still passing (no regression)
- [ ] 3 new integration tests passing
- [ ] 6 new E2E tests passing
- [ ] `npm run build` succeeds
- [ ] Total execution time < 90s for new tests
- [ ] Evidence log filled with timestamps

---

## Quick Reference: Delegation Commands

```bash
# SC1: Test Utilities
/implement "Create tests/utils/products-page-helpers.ts with SELECTORS, navigateToCategory, TEST_CATEGORIES, viewports"

# SC2: Integration Tests  
/implement "Create tests/integration/products/ProductGrid.integration.test.tsx with render and empty state tests"
/implement "Create tests/integration/products/CategoryPageClient.integration.test.tsx with product count test"

# SC3: E2E Tests
/implement "Create tests/e2e/products-page/critical-flows.spec.ts with 6 tests: navigation, 404, empty, mobile drawer, sort, skeleton"

# Verification
/test --suite all
```
