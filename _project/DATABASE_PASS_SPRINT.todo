Homepage Singleton Data Pass Sprint:

  Phase 1: Sanity Backend (The Curation Hub):
    [ ] Create `homepage` schema type with fields: featured, spotlight1, spotlight2, spotlight3, iemsGallery, newestRelease, dacs, accessoriesCables, accessoriesEarpads, accessoriesStorage.
    [ ] All fields configured as references or arrays of references to `product`.
    [ ] Add `homepage` to Sanity schema configuration.
    [ ] Update Sanity Desk Structure to render `homepage` as a Singleton (preventing multiple homepage documents).

  Phase 2: Architecture & Foundation:
    [ ] Add `export const revalidate = 3600` to `app/(store)/page.tsx`.
    [ ] Verify `Hero` component remains completely untouched.

  Phase 3: Component Data Passes (Independent Fetching):
    [ ] Featured: Query `*[_type == "homepage"][0].featured[]->` and map to carousel.
    [ ] ProductSpotlight1: Query `*[_type == "homepage"][0].spotlight1->` and map to layout.
    [ ] ProductSpotlight2: Query `*[_type == "homepage"][0].spotlight2->` and map to layout.
    [ ] ProductSpotlight3: Query `*[_type == "homepage"][0].spotlight3->` and map to layout.
    [ ] IemsGallery: Query `*[_type == "homepage"][0].iemsGallery[]->` and map to grid.
    [ ] NewestRelease: Query `*[_type == "homepage"][0].newestRelease->` (including gallery images) and map to layout.
    [ ] Dacs: Query `*[_type == "homepage"][0].dacs[]->` and map to carousel.
    [ ] Accessories: Query the three specific accessory arrays from the singleton and map to the three carousels.

  Phase 4: Final Validation:
    [ ] Run dev server.
    [ ] Populate the Homepage singleton in Sanity Studio with test products.
    [ ] Verify all mock data is replaced by database entries visually.
    [ ] Verify ordering in Studio matches frontend rendering.