export default function TypographyAliasesPage() {
  return (
    <div className="min-h-screen px-8 py-16 overflow-y-auto" style={{ background: "#070808" }}>

      <div className="py-8" style={{ borderBottom: "1px solid #4A4948" }}>
        <p style={{ fontSize: "11px", color: "#6E6D6B", marginBottom: "8px" }}>type-hero-headline — hero main headline</p>
        <p className="text-cap type-hero-headline">type-hero-headline — hero main headline</p>
      </div>

      <div className="py-8" style={{ borderBottom: "1px solid #4A4948" }}>
        <p style={{ fontSize: "11px", color: "#6E6D6B", marginBottom: "8px" }}>type-hero-sub — hero subheadline</p>
        <p className="text-cap type-hero-sub">type-hero-sub — hero subheadline</p>
      </div>

      <div className="py-8" style={{ borderBottom: "1px solid #4A4948" }}>
        <p style={{ fontSize: "11px", color: "#6E6D6B", marginBottom: "8px" }}>type-section-hed — section heading</p>
        <p className="text-cap type-section-hed">type-section-hed — section heading</p>
      </div>

      <div className="py-8" style={{ borderBottom: "1px solid #4A4948" }}>
        <p style={{ fontSize: "11px", color: "#6E6D6B", marginBottom: "8px" }}>type-section-sub — section subheading</p>
        <p className="text-cap type-section-sub">type-section-sub — section subheading</p>
      </div>

      <div className="py-8" style={{ borderBottom: "1px solid #4A4948" }}>
        <p style={{ fontSize: "11px", color: "#6E6D6B", marginBottom: "8px" }}>type-card-title — product card title</p>
        <p className="text-cap type-card-title">type-card-title — product card title</p>
      </div>

      <div className="py-8" style={{ borderBottom: "1px solid #4A4948" }}>
        <p style={{ fontSize: "11px", color: "#6E6D6B", marginBottom: "8px" }}>type-metadata — labels and metadata</p>
        <p className="text-cap type-metadata">type-metadata — labels and metadata</p>
      </div>

      <div className="py-8" style={{ borderBottom: "1px solid #4A4948" }}>
        <p style={{ fontSize: "11px", color: "#6E6D6B", marginBottom: "8px" }}>type-price — product price</p>
        <p className="text-cap type-price">$299 — type-price product price</p>
      </div>

      <div className="py-8" style={{ borderBottom: "1px solid #4A4948" }}>
        <p style={{ fontSize: "11px", color: "#6E6D6B", marginBottom: "8px" }}>type-overline — overline label</p>
        <p className="text-cap type-overline">type-overline — overline label</p>
      </div>

      <div className="py-8" style={{ borderBottom: "1px solid #4A4948" }}>
        <p style={{ fontSize: "11px", color: "#6E6D6B", marginBottom: "8px" }}>type-body — paragraph and body copy</p>
        <p className="text-cap type-body">type-body — paragraph and body copy</p>

      </div>

      <div className="py-8" style={{ borderBottom: "1px solid #4A4948" }}>
        <p style={{ fontSize: "11px", color: "#6E6D6B", marginBottom: "8px" }}>type-caption — captions and small text</p>
        <p className="text-cap type-caption">type-caption — captions and small text</p>
      </div>

    </div>
  );
}
