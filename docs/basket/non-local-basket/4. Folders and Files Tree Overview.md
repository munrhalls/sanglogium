Goal: capture all folders and files involved in creating the feature in minimalest way possible.
Criteria: 0 unnecessary verbiage, 0 unnecessary characters

# Folders and Files Tree Overview: Non-Local Basket

```
store/
├── basketStore.ts - Zustand store: manages basket state with persist middleware for localStorage and cross-tab sync

components/
├── features/basket/
│   ├── ProductPageBasketControls.tsx - component: renders add/increment/decrement buttons on product pages
│   ├── BasketPageBasketControls.tsx - component: renders increment/decrement/remove buttons on basket page
│   └── BasketButton.tsx - component: renders header basket button with badge count

app/components/features/products/
├── ProductCard.tsx - component: integrates ProductPageBasketControls for product grid items
├── ProductInfo.tsx - component: integrates ProductPageBasketControls for product detail page
└── RelatedProducts.tsx - component: integrates ProductPageBasketControls for related products section

app/(store)/search/
├── SearchResults.tsx - page: integrates ProductPageBasketControls for search results items

app/components/features/search/
├── AutocompleteItem.tsx - component: integrates ProductPageBasketControls for search autocomplete items

app/components/layout/header/
└── Header.tsx - component: integrates BasketButton for navigation
```