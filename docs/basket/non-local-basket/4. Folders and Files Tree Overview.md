Goal: capture all folders and files involved in creating the feature in minimalest way possible.
Criteria: 0 unnecessary verbiage, 0 unnecessary characters

# Folders and Files Tree Overview: Non-Local Basket

```
store/
├── basketStore.ts - Zustand store: manages basket state with persist middleware for localStorage and cross-tab sync

components/
├── features/basket/
│   ├── BasketControls.tsx - component: renders add/increment/decrement/remove buttons on product pages
│   └── BasketButton.tsx - component: renders header basket button with badge count

app/components/features/products/
├── ProductCard.tsx - component: integrates BasketControls for product grid items
├── ProductInfo.tsx - component: integrates BasketControls for product detail page
└── RelatedProducts.tsx - component: integrates BasketControls for related products section

app/(store)/search/
├── SearchResults.tsx - page: integrates BasketControls for search results items

app/components/features/search/
├── AutocompleteItem.tsx - component: integrates BasketControls for search autocomplete items

app/components/layout/header/
└── Header.tsx - component: integrates BasketButton for navigation
```