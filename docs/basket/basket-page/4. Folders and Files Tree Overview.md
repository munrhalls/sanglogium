Goal: capture all folders and files involved in creating the feature in minimalest way possible.
Criteria: 0 unnecessary verbiage, 0 unnecessary characters

# Folders and Files Tree Overview: Basket Page

```
store/
└── basketStore.ts - Zustand store: manages basket state with syncWithCMS action for CMS comparison

app/(store)/basket/
└── Basket.tsx - page: renders basket page with items table, sync status bar, checkout button, empty state

components/features/basket/
├── BasketItemsTable.tsx - component: renders table with basket items, quantity controls, comparison results
├── SyncStatusBar.tsx - component: renders sync status (loading, success, error with retry)
├── CheckoutButton.tsx - component: renders checkout button with disabled state during sync/error
├── UnavailableItems.tsx - component: renders unavailable items banner and list
└── EmptyBasket.tsx - component: renders empty state with browse products button

sanity/lib/products/
└── getProductsByIds.ts - function: fetches product data from CMS for sync comparison
```