# DEPRECATED — Use @/frame-decompose instead

`@/frame-decompose` outputs INSIDE the beads issue only, NEVER in `docs/` during active work.

See `.windsurf/workflows/frame-decompose.md` for the correct workflow.