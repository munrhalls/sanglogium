{
  "postWrite": {
    "enabled": true,
    "command": "npm run lint",
    "shell": "powershell",
    "description": "Automatically run linter on every file write to enforce zero-regression reality"
  }
}
