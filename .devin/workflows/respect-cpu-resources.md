- NEVER run the most expensive commands, e.g. npm run build, linting or typescript checks
- NEVER run any expensive commands - unless it's absolutely unavoidable 
- be aware that your work is part of simultaneous parallel agents work, who all use the same PC resources - therefore, any unnecessary drain on resources is EXTREMELY DISCOURAGED


- that does NOT MEAN the quality of the work can be lower - it must be up to 100% professional simplicity, robustness, accuracy, completion and system-alignment standard 
- that DOES MEAN the most expensive commands are NEVER RUN, and the expensive commands are only run if absolutely unavoidable 