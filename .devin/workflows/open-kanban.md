---
description: Launch the beads kanban board for sang-logium
---

```
node server.mjs --path C:\webdev\sang-logium
```

Run from `C:\webdev\beads-kanban\`, then open http://localhost:3333

If port 3333 is taken, add `--port 3334` (auto-increments on collision).

cd C:\webdev\beads-kanban\ 
node server.mjs --path C:\webdev\sang-logium
