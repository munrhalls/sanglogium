(Justin Sung's GRINDE mind mapping) as simply as possible.

10:28 PM
Here is the flat list, causal order, no prose:

INTENT CLARITY feature scope sentence one-sentence done condition boundary definition (what it is NOT)

CODEBASE GROUND TRUTH existing analogous components located real class names extracted from live files real GROQ query shapes from live files real file paths verified to exist

DESIGN SYSTEM GROUND TRUTH addComponents classes catalogued from config color token usage verified in live components typography aliases verified in live components component class vs utility class distinction known

RESEARCH QUALITY only load-bearing facts included generic knowledge excluded your-stack-specific only

COMPRESS ACCURACY no hallucinated tokens no invented file paths no suggested patterns (only verified precedents) compression source = live components, not config file

OPUS INPUT DENSITY compressed context token count intent clarity fed to Opus all three inputs present (codebase + design + intent) nothing redundant in Opus input

SPRINT DOC QUALITY scope boundaries sealed DoD verifiable in under 5 minutes Pass 1/2/3 correctly sequenced Layer 1/2/3/4 enforced per component token names are component classes not utilities file paths are real no scope touches same files as adjacent scope regression risks named explicitly

EXECUTOR MODEL CAPABILITY context window sufficient for scope size model follows sprint doc without supplementing from own knowledge model doesn't invent classes when unsure

REVIEW SPEED AND ACCURACY you know what a broken token looks like you check class names against addComponents you check file paths exist review takes under 5 minutes pass/fail decision is binary not iterative

SCOPE SIZE CALIBRATION single component per scope executor finishes without context overflow DoD checkable by visual inspection no partial implementations crossing scope boundaries

SEQUENCE INTEGRITY scope N doesn't depend on scope N+1 data layer before UI layer structure before surface server components before client components

REDO LOOP COST how often executor drifts how long re-run takes whether re-run uses corrected sprint or same sprint whether fix is in sprint doc or in your head

COGNITIVE LOAD MANAGEMENT decisions batched to planning phase no re-planning during execution your attention on review only during execution pipeline runs without your intervention between scopes

TOKEN COST CONTROL Opus called once per feature cheap model handles all extraction compress step eliminates redundant Opus input re-runs use cheap model not Opus

PIPELINE STABILITY each command produces deterministic output no step requires you to hold context from a previous step failures are localised not cascading output of each step is a file, not a mental state