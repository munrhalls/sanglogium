# Shippo API Parcel Format

```json
{
  "length": 10,
  "width": 10,
  "height": 5,
  "weight": 500,
  "distance_unit": "cm",
  "mass_unit": "g"
}
```

All 6 fields required. `distance_unit` values: cm, in. `mass_unit` values: g, oz, lb, kg.
